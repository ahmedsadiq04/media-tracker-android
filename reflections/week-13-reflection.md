# Final Reflection — ICS 342

**Name:** Sadiq Ahmed
**Due:** End of class, Week 13 (August 13, 2026)
**Submitted with:** Your final pull request
**Worth:** 50 points

---

This document is your final written submission for the course. It is not a summary of the syllabus. It is not a list of features you implemented. It is a record of what you actually learned, written by you, about your specific experience building this specific app this semester.

Responses that are vague, generic, or could have been written by someone who wasn't in class will not receive full credit. Responses that were generated by an AI and not substantially revised will be obvious. Write in your own voice. Be specific. Be honest.

There is no page minimum. There is a quality minimum. A response that takes two paragraphs to say something real is worth more than five paragraphs of filler.

---

## Part 1 — Your App

**1a. Open your final pull request on GitHub. Find the commit that you are most proud of — not the largest, not the last one, the one that meant the most to you. Paste the commit URL here and explain why you chose it. What did it take to get there? What was broken before and what worked after?**

> Your answer: https://github.com/ahmedsadiq04/media-tracker-android/pull/12 <br/>
> This was my most proud Pull-Request as this was the first week of the 'create' task where
> we had to create our own feature, and I figured out how to create a new section on teh bottom and creating quotes
> which helped me get decently good (okay level) at Kotlin and modifiers.
> Prior to this, I've had experience in React but my UI/UX experience was not the best,
> so creating this was by far the greatest moment for me throughout this project.

---

**1b. Name one screen in your app that you think is genuinely well-built. Not perfect — well-built. Explain specifically why: what design decisions did you make, what did you refactor, and how does it differ from how you would have approached it in week 2?**

> Your answer: The media details screen is prob the most well-built as its the one with
> the most features that work. In that screen, you are able to Save, Want to, and create quotes
> for the specific media while also being able to see reviews (partial functionality, can load but cant create).
> Compared to the original project, this screen looks about the same - beside adding the missing items that is
> shown in the View Template on your website, its mainly the functionality added mentioned prior.

---

**1c. Name one screen or feature that you are not satisfied with. What is wrong with it? If you had one more week, what specifically would you change?**

> Your answer: The profile page is by far the weakest on this project, and thats mainly because
> we've never touched it but also not an excuse as if this app was built from the ground up, it should've been touched.
> At the current version of the project, the UI is stale with none of the user's data being used to update their profile.
> Additionally, the settings are non-existent and a bunch of the buttons on that page, lead to empty UI
> that have no return button, where unless you swipe back, your stuck on that page. Given more time, I would keep
> the UI, but add the functionality thats needed.

---

## Part 2 — A Specific Bug

**2a. Describe the hardest bug you fixed this semester. Not the most recent one — the one that took the longest or cost you the most confusion. What was the symptom? What did you think the problem was at first? What was it actually? How did you find it?**

> Your answer: I think the hardest one was when I was learning about the HTTP requests in Java / Kotlin.
> It was my first time using retrofit (I believe this was the name for it) and the plethora of bugs that came
> with it, like forgetting to put @SERIALIZE for your data class, or if the data is not the exact same 
> as what the server returns, the app crashes - which includes nullable / empty data, so String? has to be 
> a real param for an optional value. The worst part of all this was I had no reliable way of testing it 
> quickly, so I had to re-build the app, and login just to test the functionality just for it to blow up in my face.

---

**2b. Copy and paste the specific lines of code you changed to fix it. (This can be a before/after comparison, a diff, or just the relevant snippet.) Explain in plain English what the fix does and why it works.**

> Your answer (include code): There was not a specific commit or code that existed but to continue from prior,

``` Kotlin
data class CreateUserRequest(
    val email: String,
    val password: String,
    val username: String,
    val displayName: String,
    val clientId: String,
    val clientSecret: String
)

data class TokenRequest(
    val grantType: String,
    val email: String = null,
    val password: String = null,
    val refreshToken: String = null,
    val clientId: String,
    val clientSecret: String
)

interface ApiService {
    @POST("users")
    suspend fun createUser(@Body request: CreateUserRequest): CreateUserResponse

    @POST("tokens")
    suspend fun login(@Body request: TokenRequest): TokenResponse
}
```

> Was turned into

``` Kotlin
@Serializable
data class CreateUserRequest(
    val email: String,
    val password: String,
    val username: String,
    val displayName: String,
    val clientId: String,
    val clientSecret: String
)

@Serializable
data class TokenRequest(
    val grantType: String,
    val email: String? = null,
    val password: String? = null,
    val refreshToken: String? = null,
    val clientId: String,
    val clientSecret: String
)

interface ApiService {
    @POST("users")
    suspend fun createUser(@Body request: CreateUserRequest): Response<CreateUserResponse>

    @POST("tokens")
    suspend fun login(@Body request: TokenRequest): Response<TokenRequest>
}
```

> With the changes being fixing the item so its @Serializable, adding the '?' where necessary,
> as well as putting Response<> around the items so we know what data is returned and if its valid

---

## Part 3 — What You Actually Learned

**3a. Pick the concept from this semester that took the longest to actually understand — not just to implement, but to understand. Describe what you thought it was before you understood it, what changed, and how you would explain it now to a student who was exactly where you were at the start of the semester.**

> Your answer: Honestly, the modifiers, and its not something thats super complicated to wrap my head around but its
> just the way Java is handled. Java is object-oriented so for things like how Modifier was built, its expected to be like so:
> `JavaClass.GetSingleton().Modify("change to").Scale(2.5).Build()` with it building on top of the object, which I like for some
> thing but in Modifier, its kind of annoying, where I'm more used to `sx={{Width: "50%", Height: "100%"}}` in JSX

---

**3b. Your weekly reflections had a "Still Confused" section. Look back at your early reflections — weeks 1 through 4. Find something you wrote that you were confused about then. Are you still confused about it? If not, when and how did it click? If you still are, say so honestly and describe what the sticking point is.**

(If you can't access your early reflections, describe something you remember being confused about in the first half of the semester.)

> Your answer: Looking back at my [week 3 reflection](./week-03-reflection.md), I wrote about my dislike for Gradle and how its 
> such as heavy builder, and still to this day, thats how I feel about it. Gradle is nice to have modules / dependencies, but
> just like CMake, Gradle is just so heavy with large files and a bunch of settings  that you must modify in order to get things
> to actually work, unlike my favorite implementation which is Go (Go.Mod) and Rust (Cargo), JS is also good with package.json

> I also wrote about my confusion with the Rest API's / HTTP @POSTs and how Java handles all that with the Retrofit package
> where my initial idea was that it should be a request -> response design workflow, where you would have your class setup like this

``` Kotlin
fun Example(user string, password string) {
    client = http.client();
    req = http.newRequest(config.url, "POST", json_data...)
    resp = client.do(req)
}
```

> Basically, you build the function top-down instead of having Retrofit do it for you, and that disconnect is something I still
> really don't like since it feels like its removing agency from me

---

**3c. Name one thing a pod mate said, asked, or showed you during a code review or work session that changed how you approached something. It doesn't have to be a big thing. What was it, and what did it change?**

> Your answer: During the first few weeks, Nick showed me how to get started with some of the things that Android Studio / 
> this project required specifically, which was mainly the strings.xaml. Before this, I really didn't know what it was,
> where it was, and why it was there, and he showed me what it does and where it was. This is also true for the icons that we
> needed in the drawable folder, although later one we moved to MUI icons which are native for Jetpack Compose

---

## Part 4 — Your Bonus Feature

**4a. Describe your bonus feature in one paragraph as if you were explaining it to someone who has never used an app before. What does it do? Why would a user want it?**

> Your answer: [Req Context for the App Currently] <br/>
> The quotes feature allows users to create quotes of their favorite media for themselves (private),
> or to share with others (public), including adding which page number it occurs on (books). Given this,
> you are also able to view other user's quotes and see what the community thinks of a given book

---

**4b. What was the technically hardest part of building it? Name a specific function, flow, or data structure that gave you trouble, and explain what the problem was.**

> Your answer: The hardest part that is still challenging for this section is the requests. There are a bunch of HTTP requests that must be made for it and getting them all correct and making sure the spec for the data class is right is one of the hardest things I had to do for it. It took quite a while getting the HTTP requests to flow, but also looking into how I could create my own navbar section at the bottom for Quotes was a pretty cool find.

---

**4c. Your bonus feature has tests. Open the test file and paste the test you think is most valuable — the one that would catch the most important failure. Explain what it proves and what it does not prove.**

> Your answer (include code): [N/A] <br/>
> I did not have enough time nor the knowledge to create tests in which all the tests currently are manual, meaning
> you would have to actually work your way thru the app to see what breaks. There are also logs to help out with this,
> but automatic testing is something I would add ASAP if given more time. I actually just learned (today 8/13) from Nick
> how testing happens which is an external library you have to bring into your project.

---

## Part 5 — Looking Forward

**5a. If you were going to continue developing this app after the semester ends, what would you build next and why?**

> Your answer: I would first and foremost finish the quotes feature since its almost done, adding the search / filter, as well as editing and deleting your own quotes. On top of that, I would work to continue fixing / implementing the basic features for the app, such as the profile, the settings, and adding a Dark Theme as I hate light only apps. Especially if users are to use this and they are in a dark environment, they just get flashbanged every time they open the app.

---

**5b. A friend tells you they want to learn Android development. Based specifically on your experience this semester — not what you've read, what you lived — what is the one thing you would tell them to understand before they write a single line of code?**

> Your answer: I would honestly tell them, first ask yourself why are you wanting to learn Android App Development,
> if its just to create a cross-platform app, then it might not be the best option, but if they have specific needs or honestly just want to learn, then go for it. They should first and foremost, learn the absolute basics of Java and Object Oriented Programming, as without this, you will have a hard time figuring out how things work. Then they should take a quick course or video on the way Jetpack Compose handles the UI where its layered. You have components built on top of each other using functions, with the first being the raw App (bg) and the closest view being what you actively see such as a Modal window.

---

## Grading

| Section | Points |
|:---|:---:|
| Part 1 — Your App | 12 |
| Part 2 — A Specific Bug | 12 |
| Part 3 — What You Actually Learned | 14 |
| Part 4 — Your Bonus Feature | 8 |
| Part 5 — Looking Forward | 4 |
| **Total** | **50** |

Full credit in each section requires specificity — code, names, dates, conversations, actual things that happened. Half credit for responses that are genuine but stay at the surface. No credit for responses that are generic, missing, or could have been written by someone who was not in this class.
